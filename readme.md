## Dependency
 
pip install pyqt5

pip install opencv-python (pip install opencv-python-headless (for mac)) 

pip install auxlib 
pip install conda 
pip install cytoolz 
conda 

pip install bob.extension 
pip install boost 


## Run the project 
run the run.py
